apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: barman-backups
  namespace: databases
spec:
  cluster:
    name: postgres18-immich
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
