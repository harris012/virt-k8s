apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: stirling-pdf-scaler
spec:
  scaleTargetRef:
    name: stirling-pdf
  minReplicaCount: 0
  maxReplicaCount: 1
  cooldownPeriod: 30
  triggers:
    - type: external-push
      metadata:
        scalerAddress: keda-add-ons-http-external-scaler.observability:9090
        interceptorRoute: keda-add-ons-http-interceptor-proxy
