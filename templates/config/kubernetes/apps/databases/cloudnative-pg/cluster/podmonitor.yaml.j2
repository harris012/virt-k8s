apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgres18-immich
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: postgres18-immich
  podMetricsEndpoints:
  - port: metrics
