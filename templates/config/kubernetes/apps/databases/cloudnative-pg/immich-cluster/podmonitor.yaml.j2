apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgres18-immich
  namespace: databases
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: postgres18-immich
  podMetricsEndpoints:
  - port: metrics
